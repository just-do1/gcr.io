# This is a basic workflow to help you get started with Actions

name: GItGoogleContainer

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      # - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Login in Docker Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Docker pull and push
        run: |
          echo "abc"
          # docker pull golang:1.24-alpine
          # docker tag golang:1.24-alpine ghcr.io/just-do1/golang:1.24-alpine
          # docker push ghcr.io/just-do1/golang:1.24-alpine

          # docker pull alpine:latest
          # docker tag alpine:latest ghcr.io/just-do1/alpine:latest
          # docker push ghcr.io/just-do1/alpine:latest

          docker pull node:18-alpine
          docker tag node:18-alpine ghcr.io/just-do1/node:18-alpine
          docker push ghcr.io/just-do1/node:18-alpine

          docker pull nginx:alpine
          docker tag nginx:alpine ghcr.io/just-do1/nginx:alpine
          docker push ghcr.io/just-do1/nginx:alpine

          
          # docker pull bitnami/postgresql:latest
          # docker pull bitnami/os-shell:latest
          # docker pull bitnami/postgres-exporter:latest
          
          # docker tag bitnami/postgresql:latest ghcr.io/just-do1/bitnami/postgresql:latest
          # docker tag bitnami/os-shell:latest ghcr.io/just-do1/bitnami/os-shell:latest
          # docker tag bitnami/postgres-exporter:latest ghcr.io/just-do1/bitnami/postgres-exporter:latest

          # docker push ghcr.io/just-do1/bitnami/postgresql:latest
          # docker push ghcr.io/just-do1/bitnami/os-shell:latest
          # docker push ghcr.io/just-do1/bitnami/postgres-exporter:latest
