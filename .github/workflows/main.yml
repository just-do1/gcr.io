# This is a basic workflow to help you get started with Actions

name: GItGoogleContainer

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      # - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Login in Docker Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Docker pull and push
        run: |
          echo "abc"
          # docker pull golang:1.23.6
          # docker tag golang:1.23.6 ghcr.io/just-do1/sig-storage/golang:1.23.6
          # docker push ghcr.io/just-do1/sig-storage/golang:1.23.6
          docker pull postgres:15-alpine
          docker pull redis:6-alpine
          docker pull semitechnologies/weaviate:1.19.0
          docker pull langgenius/dify-sandbox:0.2.12
          docker pull ubuntu/squid:latest
          docker pull langgenius/dify-api:1.4.0
          docker pull langgenius/dify-api:1.4.0
          docker pull langgenius/dify-web:1.4.0
          docker pull nginx:stable
          docker pull postgres:15-alpine
          docker pull langgenius/dify-plugin-daemon:0.1.0-local

          docker tag postgres:15-alpine ghcr.io/just-do1/dify/postgres:15-alpine
          docker tag redis:6-alpine ghcr.io/just-do1/dify/redis:6-alpine
          docker tag semitechnologies/weaviate:1.19.0 ghcr.io/just-do1/dify/weaviate:1.19.0
          docker tag langgenius/dify-sandbox:0.2.12 ghcr.io/just-do1/dify/dify-sandbox:0.2.12
          docker tag ubuntu/squid:latest ghcr.io/just-do1/dify/squid:latest
          docker tag langgenius/dify-api:1.4.0 ghcr.io/just-do1/dify/dify-api:1.4.0
          docker tag langgenius/dify-api:1.4.0 ghcr.io/just-do1/dify/dify-api:1.4.0
          docker tag langgenius/dify-web:1.4.0 ghcr.io/just-do1/dify/dify-web:1.4.0
          docker tag nginx:stable ghcr.io/just-do1/dify/nginx:stable
          docker tag postgres:15-alpine ghcr.io/just-do1/dify/postgres:15-alpine
          docker tag langgenius/dify-plugin-daemon:0.1.0-local ghcr.io/just-do1/dify/dify-plugin-daemon:0.1.0-local

          docker push ghcr.io/just-do1/dify/postgres:15-alpine
          docker push ghcr.io/just-do1/dify/redis:6-alpine
          docker push ghcr.io/just-do1/dify/weaviate:1.19.0
          docker push ghcr.io/just-do1/dify/dify-sandbox:0.2.12
          docker push ghcr.io/just-do1/dify/squid:latest
          docker push ghcr.io/just-do1/dify/dify-api:1.4.0
          docker push ghcr.io/just-do1/dify/dify-api:1.4.0
          docker push ghcr.io/just-do1/dify/dify-web:1.4.0
          docker push ghcr.io/just-do1/dify/nginx:stable
          docker push ghcr.io/just-do1/dify/postgres:15-alpine
          docker push ghcr.io/just-do1/dify/dify-plugin-daemon:0.1.0-local

      # - name: download
      #   run: |
      #     wget https://ppc.land/content/files/2025/01/Newwhitepaper_Agents2.pdf
      # - name: Upload GitHub Pages artifact
      #   uses: actions/upload-pages-artifact@v3.0.1
      #   with:
      #     # Artifact name
      #     name: Newwhitepaper_Agents2.pdf
      #     # Path of the directory containing the static assets.
      #     path: ./
      #     # Duration after which artifact will expire in days.
      #     retention-days: 1
          
          
